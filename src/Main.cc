#include "LPQ.h"
#include <iostream>

// using lpq::LowPrecisionQuantizer;

int main() {

std::cout << "CALL BEFORE lpq instantiation ..." << std::endl;
//   auto quantizer = LowPrecisionQuantizer<int8_t>(/* bit_width = */ 8);

  std::cout << "[EXECUTING] Main.cc" << std::endl;
  return 0;
}